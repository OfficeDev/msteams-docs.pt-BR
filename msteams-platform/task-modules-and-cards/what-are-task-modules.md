---
title: O que são módulos de tarefas?
author: clearab
description: Adicione experiências de Popup modais para coletar ou exibir informações para seus usuários de seus aplicativos do Microsoft Teams.
ms.topic: overview
ms.author: anclear
ms.openlocfilehash: 22fdc7a9dab1ff6f27e2b0d144e54676b6cca50e
ms.sourcegitcommit: 4329a94918263c85d6c65ff401f571556b80307b
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 02/01/2020
ms.locfileid: "41672888"
---
# <a name="what-are-task-modules"></a><span data-ttu-id="49762-103">O que são módulos de tarefas?</span><span class="sxs-lookup"><span data-stu-id="49762-103">What are task modules?</span></span>

<span data-ttu-id="49762-104">Os módulos de tarefas permitem que você crie experiências pop-up restritas em seu aplicativo do Microsoft Teams.</span><span class="sxs-lookup"><span data-stu-id="49762-104">Task modules allow you to create modal popup experiences in your Teams application.</span></span> <span data-ttu-id="49762-105">Dentro do pop-up, você pode executar seu próprio código HTML/JavaScript personalizado `<iframe>`, mostrar um widget baseado em um YouTube ou Microsoft Stream Video ou exibir um [cartão adaptável](/adaptive-cards/).</span><span class="sxs-lookup"><span data-stu-id="49762-105">Inside the popup you can run your own custom HTML/JavaScript code, show an `<iframe>`-based widget such as a YouTube or Microsoft Stream video or display an [Adaptive card](/adaptive-cards/).</span></span> <span data-ttu-id="49762-106">Eles são especialmente úteis para iniciar e concluir tarefas ou exibir informações ricas, como vídeos ou painéis do Power BI.</span><span class="sxs-lookup"><span data-stu-id="49762-106">They are especially useful for initiating and completing tasks or displaying rich information like videos or Power BI dashboards.</span></span> <span data-ttu-id="49762-107">Uma experiência de pop-up geralmente é mais natural para usuários que iniciam e concluem tarefas comparadas a uma guia ou uma experiência de bot baseada em conversas.</span><span class="sxs-lookup"><span data-stu-id="49762-107">A popup experience is often more natural for users initiating and completing tasks compared to a tab or a conversation-based bot experience.</span></span>

<span data-ttu-id="49762-108">Os módulos de tarefas são criados na base das guias do Microsoft Teams; Eles são essencialmente uma guia dentro de uma janela pop-up.</span><span class="sxs-lookup"><span data-stu-id="49762-108">Task modules build on the foundation of Microsoft Teams tabs; they are essentially a tab inside a popup window.</span></span> <span data-ttu-id="49762-109">Eles usam o mesmo SDK, portanto, se você tiver criado uma guia, já terá 90% da maneira de poder criar um módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-109">They use the same SDK, so if you've built a tab you are already 90% of the way to being able to create a task module.</span></span>

<span data-ttu-id="49762-110">Os módulos de tarefa podem ser invocados de três maneiras:</span><span class="sxs-lookup"><span data-stu-id="49762-110">Task modules can be invoked in three ways:</span></span>

* <span data-ttu-id="49762-111">**Guias de canal ou pessoais.**</span><span class="sxs-lookup"><span data-stu-id="49762-111">**Channel or personal tabs.**</span></span> <span data-ttu-id="49762-112">Usando o SDK de guias do Microsoft Teams, você pode chamar módulos de tarefa de botões, links ou menus na sua guia. [isso é abordado em detalhes aqui.](~/task-modules-and-cards/task-modules/task-modules-tabs.md)</span><span class="sxs-lookup"><span data-stu-id="49762-112">Using the Microsoft Teams Tabs SDK you can invoke task modules from buttons, links or menus on your tab. [This is covered in detail here.](~/task-modules-and-cards/task-modules/task-modules-tabs.md)</span></span>
* <span data-ttu-id="49762-113">**Robôs.**</span><span class="sxs-lookup"><span data-stu-id="49762-113">**Bots.**</span></span> <span data-ttu-id="49762-114">Botões de [cartões](~/task-modules-and-cards/cards/cards-reference.md) enviados do bot.</span><span class="sxs-lookup"><span data-stu-id="49762-114">Buttons on [cards](~/task-modules-and-cards/cards/cards-reference.md) sent from your bot.</span></span> <span data-ttu-id="49762-115">Isso é particularmente útil quando você não precisa de todos em um canal para ver o que está fazendo com um bot.</span><span class="sxs-lookup"><span data-stu-id="49762-115">This is particularly useful when you don't need everyone in a channel to see what you are doing with a bot.</span></span> <span data-ttu-id="49762-116">Por exemplo, quando os usuários respondem a uma pesquisa em um canal, não é extremamente útil ver um registro dessa pesquisa que está sendo criada.</span><span class="sxs-lookup"><span data-stu-id="49762-116">For example, when having users respond to a poll in a channel it's not terribly useful to see a record of that poll being created.</span></span> [<span data-ttu-id="49762-117">Isso é abordado em detalhes aqui.</span><span class="sxs-lookup"><span data-stu-id="49762-117">This is covered in detail here.</span></span>](~/task-modules-and-cards/task-modules/task-modules-bots.md)
* <span data-ttu-id="49762-118">**Fora do teams de um link profundo.**</span><span class="sxs-lookup"><span data-stu-id="49762-118">**Outside of Teams from a deep link.**</span></span> <span data-ttu-id="49762-119">Você também pode criar URLs para chamar um módulo de tarefa de qualquer lugar.</span><span class="sxs-lookup"><span data-stu-id="49762-119">You can also create URLs to invoke a task module from anywhere.</span></span> [<span data-ttu-id="49762-120">Isso é abordado em detalhes aqui.</span><span class="sxs-lookup"><span data-stu-id="49762-120">This is covered in detail here.</span></span>](#task-module-deep-link-syntax)

## <a name="what-a-task-module-looks-like"></a><span data-ttu-id="49762-121">A aparência de um módulo de tarefa</span><span class="sxs-lookup"><span data-stu-id="49762-121">What a task module looks like</span></span>

<span data-ttu-id="49762-122">Veja a seguir a aparência de um módulo de tarefa quando invocado de um bot (sem os retângulos coloridos e círculos numerados, naturalmente):</span><span class="sxs-lookup"><span data-stu-id="49762-122">Here's what a task module looks like when invoked from a bot (without the colored rectangles and numbered circles, of course):</span></span>

![Exemplo de módulo de tarefa](~/assets/images/task-module/task-module-example.png)

<span data-ttu-id="49762-124">Vamos orientá-lo:</span><span class="sxs-lookup"><span data-stu-id="49762-124">Let's walk through it:</span></span>

1. <span data-ttu-id="49762-125">O [ `color` ícone](~/resources/schema/manifest-schema.md#icons)do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="49762-125">Your app's [`color` icon](~/resources/schema/manifest-schema.md#icons).</span></span>
2. <span data-ttu-id="49762-126">O [ `short` nome](~/resources/schema/manifest-schema.md#name)do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="49762-126">Your app's [`short` name](~/resources/schema/manifest-schema.md#name).</span></span>
3. <span data-ttu-id="49762-127">O título do módulo de tarefa especificado na `title` Propriedade do [objeto TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="49762-127">The task module's title specified in the `title` property of the [TaskInfo object](#the-taskinfo-object).</span></span>
4. <span data-ttu-id="49762-128">O botão fechar/cancelar do módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-128">The task module's close/cancel button.</span></span> <span data-ttu-id="49762-129">Se o usuário pressionar isso, seu aplicativo receberá um `err` evento conforme descrito [aqui](~/task-modules-and-cards/task-modules/task-modules-tabs.md#example-submitting-the-result-of-a-task-module).</span><span class="sxs-lookup"><span data-stu-id="49762-129">If the user presses this, your app will receive an `err` event as described [here](~/task-modules-and-cards/task-modules/task-modules-tabs.md#example-submitting-the-result-of-a-task-module).</span></span> <span data-ttu-id="49762-130">(**Observação:** no momento, não é possível detectar esse evento quando um módulo de tarefa é chamado de um bot.)</span><span class="sxs-lookup"><span data-stu-id="49762-130">(**Note:** It is currently not possible to detect this event when a task module is invoked from a bot.)</span></span>
5. <span data-ttu-id="49762-131">O retângulo azul é o onde sua página da Web aparece se você estiver carregando sua própria página da Web `url` usando a propriedade do [objeto TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="49762-131">The blue rectangle is the where your web page appears if you are loading your own web page using the `url` property of the [TaskInfo object](#the-taskinfo-object).</span></span> <span data-ttu-id="49762-132">Mais detalhes estão na seção [tamanho do módulo de tarefas](#task-module-sizing) abaixo.</span><span class="sxs-lookup"><span data-stu-id="49762-132">More detail is in the [task module sizing](#task-module-sizing) section below.</span></span>
6. <span data-ttu-id="49762-133">Se você estiver exibindo um cartão adaptável por meio `card` da Propriedade do [objeto TaskInfo](#the-taskinfo-object) , o preenchimento será adicionado para você, caso contrário, você precisará [lidar com isso](#task-module-css-for-htmljavascript-task-modules).</span><span class="sxs-lookup"><span data-stu-id="49762-133">If you are displaying an Adaptive card via the `card` property of the [TaskInfo object](#the-taskinfo-object) the padding is added for you, otherwise you'll need to [handle this yourself](#task-module-css-for-htmljavascript-task-modules).</span></span>
7. <span data-ttu-id="49762-134">Os botões de cartões adaptáveis serão renderizados aqui.</span><span class="sxs-lookup"><span data-stu-id="49762-134">Adaptive card buttons will render here.</span></span> <span data-ttu-id="49762-135">Se estiver usando sua própria página, você deve criar seus próprios botões.</span><span class="sxs-lookup"><span data-stu-id="49762-135">If you're using your own page you must create your own buttons.</span></span>

## <a name="overview-of-invoking-and-dismissing-task-modules"></a><span data-ttu-id="49762-136">Visão geral de chamar e descartar módulos de tarefa</span><span class="sxs-lookup"><span data-stu-id="49762-136">Overview of invoking and dismissing task modules</span></span>

<span data-ttu-id="49762-137">Os módulos de tarefa podem ser invocados de guias, bots ou links profundas e o que aparece em um pode ser HTML ou um cartão adaptável, portanto, há muita flexibilidade em termos de como eles são invocados e como lidar com o resultado da interação de um usuário.</span><span class="sxs-lookup"><span data-stu-id="49762-137">Task modules can be invoked from tabs, bots or deep links and what appears in one can be either HTML or an Adaptive card, so there's a lot of flexibility in terms of how they are invoked and how to deal with the result of a user's interaction.</span></span> <span data-ttu-id="49762-138">A tabela a seguir resume como isso funciona:</span><span class="sxs-lookup"><span data-stu-id="49762-138">The table below summarizes how this works:</span></span>

| <span data-ttu-id="49762-139">**Invocado via...**</span><span class="sxs-lookup"><span data-stu-id="49762-139">**Invoked via...**</span></span> | <span data-ttu-id="49762-140">**O módulo de tarefa é HTML/JavaScript**</span><span class="sxs-lookup"><span data-stu-id="49762-140">**Task module is HTML/JavaScript**</span></span> | <span data-ttu-id="49762-141">**O módulo de tarefa é um cartão adaptável**</span><span class="sxs-lookup"><span data-stu-id="49762-141">**Task module is Adaptive card**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="49762-142">**JavaScript em uma guia**</span><span class="sxs-lookup"><span data-stu-id="49762-142">**JavaScript in a tab**</span></span> | <span data-ttu-id="49762-143">1. Use a função `tasks.startTask()` SDK do teams com uma `submitHandler(err, result)` função de retorno de chamada opcional</span><span class="sxs-lookup"><span data-stu-id="49762-143">1. Use the Teams client SDK function `tasks.startTask()` with an optional `submitHandler(err, result)` callback function</span></span> <br/><br/> <span data-ttu-id="49762-144">2. no código do módulo de tarefa, quando o usuário terminar, chame a função `tasks.submitTask()` do SDK do teams com um `result` objeto como um parâmetro.</span><span class="sxs-lookup"><span data-stu-id="49762-144">2. In the task module code, when the user is finished, call the Teams SDK function `tasks.submitTask()` with a `result` object as a parameter.</span></span> <span data-ttu-id="49762-145">Se um `submitHandler` retorno de chamada tiver `tasks.startTask()`sido especificado no, o `result` Teams o chamará como um parâmetro.</span><span class="sxs-lookup"><span data-stu-id="49762-145">If a `submitHandler` callback was specified in `tasks.startTask()`, Teams calls it with `result` as a parameter.</span></span><br/><br/> <span data-ttu-id="49762-146">3. se houve um erro ao invocar `tasks.startTask()`, a `submitHandler` função é chamada com uma `err` cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="49762-146">3. If there was an error when invoking `tasks.startTask()`, the `submitHandler` function is called with an `err` string instead.</span></span> <br/><br/> <span data-ttu-id="49762-147">4. você também pode especificar um `completionBotId` quando chamado `teams.startTask()` -, caso `result` seja enviado ao bot.</span><span class="sxs-lookup"><span data-stu-id="49762-147">4. You can also specify a `completionBotId` when calling `teams.startTask()` - in that case `result` is sent to the bot instead.</span></span> | <span data-ttu-id="49762-148">1. chame a função `tasks.startTask()` de SDK do cliente do teams com um [objeto TaskInfo](#the-taskinfo-object) e `TaskInfo.card` que contém o JSON para o cartão adaptável a ser mostrado no pop-up módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-148">1. Call the Teams client SDK function `tasks.startTask()` with a [TaskInfo object](#the-taskinfo-object) and `TaskInfo.card` containing the JSON for the Adaptive card to show in the task module popup.</span></span> <br/><br/> <span data-ttu-id="49762-149">2. se um `submitHandler` retorno de chamada tiver `tasks.startTask()`sido especificado no, o Teams o chamará com uma `err` cadeia de caracteres `tasks.startTask()` se houver um erro ao invocar ou se o usuário fechar o pop-up de módulo de tarefa usando o X no canto superior direito.</span><span class="sxs-lookup"><span data-stu-id="49762-149">2. If a `submitHandler` callback was specified in `tasks.startTask()`, Teams calls it with an `err` string if there was an error when invoking `tasks.startTask()` or if the user closes the task module popup using the X at the upper right.</span></span> <br/><br/> <span data-ttu-id="49762-150">3. se o usuário pressiona um botão Action. Submit, seu `data` objeto é retornado como o valor de. `result`</span><span class="sxs-lookup"><span data-stu-id="49762-150">3. If the user presses an Action.Submit button then its `data` object is returned as the value of `result`.</span></span> |
| <span data-ttu-id="49762-151">**Botão cartão de bot**</span><span class="sxs-lookup"><span data-stu-id="49762-151">**Bot card button**</span></span> | <span data-ttu-id="49762-152">1. os botões de cartão de bot, dependendo do tipo de botão, podem chamar módulos de tarefas de duas maneiras: uma URL de link profundo ou `task/fetch` enviando uma mensagem.</span><span class="sxs-lookup"><span data-stu-id="49762-152">1. Bot card buttons, depending on the type of button, can invoke task modules in two ways: a deep link URL or by sending a `task/fetch` message.</span></span> <span data-ttu-id="49762-153">Veja abaixo como funcionam as URLs de link profundo.</span><span class="sxs-lookup"><span data-stu-id="49762-153">See below for how deep link URLs work.</span></span> <br/><br/> <span data-ttu-id="49762-154">2. se `type` a ação do botão for `task/fetch` (`Action.Submit` tipo de botão para cartões adaptáveis), `task/fetch invoke` um evento (um post http nas capas) será enviado ao bot, e o bot responderá à postagem com http 200 e o corpo da resposta contendo um invólucro em torno do [objeto TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="49762-154">2. If the button's action `type` is `task/fetch` (`Action.Submit` button type for Adaptive cards), a `task/fetch invoke` event (an HTTP POST under the covers) is sent to the bot, and the bot responds to the POST with HTTP 200 and the response body containing a wrapper around the [TaskInfo object](#the-taskinfo-object).</span></span> <span data-ttu-id="49762-155">Isso é explicado em detalhes sobre como [invocar um módulo de tarefa por meio de tarefa/busca](~/task-modules-and-cards/task-modules/task-modules-bots.md#invoking-a-task-module-via-taskfetch).</span><span class="sxs-lookup"><span data-stu-id="49762-155">This is explained in detail in [invoking a task module via task/fetch](~/task-modules-and-cards/task-modules/task-modules-bots.md#invoking-a-task-module-via-taskfetch).</span></span><br/><br/> <span data-ttu-id="49762-156">3. o Microsoft Teams exibe o módulo de tarefa; Quando o usuário terminar, chame a função `tasks.submitTask()` SDK do teams com `result` um objeto como um parâmetro.</span><span class="sxs-lookup"><span data-stu-id="49762-156">3. Teams displays the task module; when the user is finished, call the Teams SDK function `tasks.submitTask()` with a `result` object as a parameter.</span></span> <br/><br/> <span data-ttu-id="49762-157">4. o bot recebe uma `task/submit invoke` mensagem que contém o `result` objeto.</span><span class="sxs-lookup"><span data-stu-id="49762-157">4. The bot receives a `task/submit invoke` message that contains the `result` object.</span></span> <span data-ttu-id="49762-158">Você tem três maneiras diferentes de responder à `task/submit` mensagem: fazendo nada (a tarefa foi concluída com êxito), exibindo uma mensagem para o usuário em uma janela pop-up ou invocando outra janela de módulo de tarefa (ou seja, criando uma experiência parecida com o assistente).</span><span class="sxs-lookup"><span data-stu-id="49762-158">You have three different ways to respond to the `task/submit` message: by doing nothing (the task completed successfully), by displaying a message to the user in a popup window, or by invoking another task module window (i.e. creating a wizard-like experience).</span></span> <span data-ttu-id="49762-159">Essas três opções são discutidas mais [na discussão detalhada sobre a tarefa/envio](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span><span class="sxs-lookup"><span data-stu-id="49762-159">These three options are discussed more [in the detailed discussion on task/submit](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span></span> | <span data-ttu-id="49762-160">1. como os botões em cartões de estrutura de bot, os botões de cartões adaptáveis dão suporte a duas maneiras de chamar módulos de `Action.openUrl` tarefa: URLs de `task/fetch` link `Action.Submit` profundo com botões e usando os botões.</span><span class="sxs-lookup"><span data-stu-id="49762-160">1. Like buttons on Bot Framework cards, buttons on Adaptive cards support two ways of invoking task modules: deep link URLs with `Action.openUrl` buttons, and via `task/fetch` using `Action.Submit` buttons.</span></span> <br/><br/> <span data-ttu-id="49762-161">2. os módulos de tarefa com cartões adaptáveis funcionam de forma semelhante ao caso HTML/JavaScript (consulte esquerda).</span><span class="sxs-lookup"><span data-stu-id="49762-161">2. Task modules with Adaptive cards work very similarly to the HTML/JavaScript case (see left).</span></span> <span data-ttu-id="49762-162">A principal diferença é que, como não há JavaScript quando você está usando cartões adaptáveis, não há como chamar `tasks.submitTask()`.</span><span class="sxs-lookup"><span data-stu-id="49762-162">The major difference is that since there's no JavaScript when you're using Adaptive cards, there's no way to call `tasks.submitTask()`.</span></span> <span data-ttu-id="49762-163">Em vez disso, o `data` Microsoft Teams pega o objeto `Action.Submit` e o retorna como a `task/submit` carga de no evento, conforme descrito [aqui](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span><span class="sxs-lookup"><span data-stu-id="49762-163">Instead, Teams takes the `data` object from `Action.Submit` and returns it as the payload of in the `task/submit` event, as described [here](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span></span> |
| <span data-ttu-id="49762-164">**URL de link profundo**</span><span class="sxs-lookup"><span data-stu-id="49762-164">**Deep link URL**</span></span> <br/>[<span data-ttu-id="49762-165">Sintaxe da URL</span><span class="sxs-lookup"><span data-stu-id="49762-165">URL syntax</span></span>](#task-module-deep-link-syntax) | <span data-ttu-id="49762-166">1. Teams invoca o módulo de tarefa; a URL que aparece dentro do `<iframe>` especificado no `url` parâmetro do link profundo.</span><span class="sxs-lookup"><span data-stu-id="49762-166">1. Teams invokes the task module; the URL that appears inside the `<iframe>` specified in the `url` parameter of the deep link.</span></span> <span data-ttu-id="49762-167">Não há nenhum `submitHandler` retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="49762-167">There is no `submitHandler` callback.</span></span> <br/><br/> <span data-ttu-id="49762-168">2. dentro do JavaScript da página no módulo de tarefa, chame `tasks.submitTask()` para fechá-lo com um `result` objeto como um parâmetro, o mesmo que ao chamá-lo de um botão de guia ou cartão de bot.</span><span class="sxs-lookup"><span data-stu-id="49762-168">2. Within the JavaScript of the page in the task module, call `tasks.submitTask()` to close it with a `result` object as a parameter, the same as when invoking it from a tab or a bot card button.</span></span> <span data-ttu-id="49762-169">No entanto, a lógica de conclusão é um pouco diferente.</span><span class="sxs-lookup"><span data-stu-id="49762-169">Completion logic is slightly different, however.</span></span> <span data-ttu-id="49762-170">Se a lógica de conclusão residir no cliente (ou seja, se não houver nenhum bot), não há `submitHandler` nenhum retorno de chamada, portanto, qualquer lógica de conclusão deve estar no código `tasks.submitTask()`que antecede a chamada.</span><span class="sxs-lookup"><span data-stu-id="49762-170">If your completion logic resides on the client (i.e. if there is no bot) there is no `submitHandler` callback, so any completion logic must be in the code preceding the call to `tasks.submitTask()`.</span></span> <span data-ttu-id="49762-171">Erros de invocação são relatados pelo console.</span><span class="sxs-lookup"><span data-stu-id="49762-171">Invocation errors are only reported via the console.</span></span> <span data-ttu-id="49762-172">Se você tiver um bot, poderá especificar um `completionBotId` parâmetro no link profundo para enviar o `result` objeto por um `task/submit` evento.</span><span class="sxs-lookup"><span data-stu-id="49762-172">If you have a bot, then you can specify a `completionBotId` parameter in the deep link to send the `result` object via a `task/submit` event.</span></span> | <span data-ttu-id="49762-173">1. Teams invoca o módulo de tarefa; o corpo do cartão JSON do cartão adaptável é especificado como um valor de codificação de URL do `card` parâmetro do link profundo.</span><span class="sxs-lookup"><span data-stu-id="49762-173">1. Teams invokes the task module; the JSON card body of the Adaptive card is specified as a URL-encoded value of the `card` parameter of the deep link.</span></span> <br/><br/> <span data-ttu-id="49762-174">2. o usuário fecha o módulo de tarefa clicando no X no canto superior direito do módulo de tarefa ou pressionando um `Action.Submit` botão no cartão.</span><span class="sxs-lookup"><span data-stu-id="49762-174">2. The user closes the task module by clicking on the X at the upper right of the task module or by pressing an `Action.Submit` button on the card.</span></span> <span data-ttu-id="49762-175">Como não há `submitHandler` uma chamada, você deve ter um bot para enviar o valor dos campos de cartão adaptável para.</span><span class="sxs-lookup"><span data-stu-id="49762-175">Since there is no `submitHandler` to call, you must have a bot to send the value of the Adaptive card fields to.</span></span> <span data-ttu-id="49762-176">Use o `completionBotId` parâmetro no link profundo para especificar o bot para o qual enviar os dados por meio de `task/submit invoke` um evento.</span><span class="sxs-lookup"><span data-stu-id="49762-176">You use the `completionBotId` parameter in the deep link to specify the bot to send the data to via a `task/submit invoke` event.</span></span> |

> [!NOTE]
> <span data-ttu-id="49762-177">Não há suporte para a invocação de um módulo de tarefa do JavaScript no Mobile.</span><span class="sxs-lookup"><span data-stu-id="49762-177">Invoking a task module from JavaScript is not supported on mobile.</span></span>

## <a name="the-taskinfo-object"></a><span data-ttu-id="49762-178">O objeto TaskInfo</span><span class="sxs-lookup"><span data-stu-id="49762-178">The TaskInfo object</span></span>

<span data-ttu-id="49762-179">O `TaskInfo` objeto contém os metadados de um módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-179">The `TaskInfo` object contains the metadata for a task module.</span></span> <span data-ttu-id="49762-180">A definição do objeto está abaixo.</span><span class="sxs-lookup"><span data-stu-id="49762-180">The object definition is below.</span></span> <span data-ttu-id="49762-181">Você **deve** definir `url` (para o iframe incorporado de ENE) `card` ou (para um cartão adaptável).</span><span class="sxs-lookup"><span data-stu-id="49762-181">You **must** define either `url` (for en embedded iFrame) or `card` (for an Adaptive Card).</span></span>

| <span data-ttu-id="49762-182">Atributo</span><span class="sxs-lookup"><span data-stu-id="49762-182">Attribute</span></span> | <span data-ttu-id="49762-183">Tipo</span><span class="sxs-lookup"><span data-stu-id="49762-183">Type</span></span> | <span data-ttu-id="49762-184">Descrição</span><span class="sxs-lookup"><span data-stu-id="49762-184">Description</span></span> |
| --- | --- | --- |
| `title` | <span data-ttu-id="49762-185">string</span><span class="sxs-lookup"><span data-stu-id="49762-185">string</span></span> | <span data-ttu-id="49762-186">Aparece abaixo do nome do aplicativo e à direita do ícone do aplicativo</span><span class="sxs-lookup"><span data-stu-id="49762-186">Appears below the app name and to the right of the app icon</span></span> |
| `height` | <span data-ttu-id="49762-187">número ou cadeia de caracteres</span><span class="sxs-lookup"><span data-stu-id="49762-187">number or string</span></span> | <span data-ttu-id="49762-188">Pode ser um número que representa a altura do módulo de tarefa em pixels, `small`ou `medium`, ou `large`.</span><span class="sxs-lookup"><span data-stu-id="49762-188">This can be a number representing the task module's height in pixels, or `small`, `medium`, or `large`.</span></span> <span data-ttu-id="49762-189">[Veja abaixo como a altura e a largura são manipuladas](#task-module-sizing).</span><span class="sxs-lookup"><span data-stu-id="49762-189">[See below for how height and width are handled](#task-module-sizing).</span></span> |
| `width` | <span data-ttu-id="49762-190">número ou cadeia de caracteres</span><span class="sxs-lookup"><span data-stu-id="49762-190">number or string</span></span> | <span data-ttu-id="49762-191">Pode ser um número que representa a largura do módulo de tarefa em pixels, `small`ou `medium`, ou `large`.</span><span class="sxs-lookup"><span data-stu-id="49762-191">This can be a number representing the task module's width in pixels, or `small`, `medium`, or `large`.</span></span> <span data-ttu-id="49762-192">[Veja abaixo como a altura e a largura são manipuladas](#task-module-sizing).</span><span class="sxs-lookup"><span data-stu-id="49762-192">[See below for how height and width are handled](#task-module-sizing).</span></span> |
| `url` | <span data-ttu-id="49762-193">string</span><span class="sxs-lookup"><span data-stu-id="49762-193">string</span></span> | <span data-ttu-id="49762-194">A URL da página carregada como `<iframe>` dentro do módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-194">The URL of the page loaded as an `<iframe>` inside the task module.</span></span> <span data-ttu-id="49762-195">O domínio da URL deve estar na [matriz validDomains](~/resources/schema/manifest-schema.md#validdomains) do aplicativo no manifesto do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="49762-195">The URL's domain must be in the app's [validDomains array](~/resources/schema/manifest-schema.md#validdomains) in your app's manifest.</span></span> |
| `card` | <span data-ttu-id="49762-196">Cartão adaptável ou um anexo de cartão de bot de cartão adaptável</span><span class="sxs-lookup"><span data-stu-id="49762-196">Adaptive card or an Adaptive card bot card attachment</span></span> | <span data-ttu-id="49762-197">O JSON para o cartão adaptável que aparece no módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-197">The JSON for the Adaptive card to appear in the task module.</span></span> <span data-ttu-id="49762-198">Se você estiver invocando de um bot, precisará usar o cartão de readaptação JSON em um objeto de estrutura `attachment` de bot.</span><span class="sxs-lookup"><span data-stu-id="49762-198">If you're invoking from a bot, you'll need to use the Adaptive card JSON in a Bot Framework `attachment` object.</span></span> <span data-ttu-id="49762-199">Em uma guia, você usará apenas um cartão adaptável.</span><span class="sxs-lookup"><span data-stu-id="49762-199">From a tab you'll use just an Adaptive Card.</span></span> [<span data-ttu-id="49762-200">Veja um exemplo.</span><span class="sxs-lookup"><span data-stu-id="49762-200">Here's an example.</span></span>](#adaptive-card-or-adaptive-card-bot-card-attachment) |
| `fallbackUrl` | <span data-ttu-id="49762-201">string</span><span class="sxs-lookup"><span data-stu-id="49762-201">string</span></span> | <span data-ttu-id="49762-202">Se um cliente não oferecer suporte ao recurso do módulo de tarefa, essa URL será aberta em uma guia do navegador.</span><span class="sxs-lookup"><span data-stu-id="49762-202">If a client does not support the task module feature, this URL is opened in a browser tab.</span></span> |
| `completionBotId` | <span data-ttu-id="49762-203">string</span><span class="sxs-lookup"><span data-stu-id="49762-203">string</span></span> | <span data-ttu-id="49762-204">Especifica uma ID do aplicativo bot para enviar o resultado da interação do usuário com o módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-204">Specifies a bot App ID to send the result of the user's interaction with the task module to.</span></span> <span data-ttu-id="49762-205">Se especificado, o bot receberá um `task/submit invoke` evento com um objeto JSON na carga do evento.</span><span class="sxs-lookup"><span data-stu-id="49762-205">If specified, the bot will receive a `task/submit invoke` event with a JSON object in the event payload.</span></span> |

> [!NOTE]
> <span data-ttu-id="49762-206">O recurso de módulo de tarefa requer que os domínios de qualquer URL que você deseja carregar estejam incluídos `validDomains` na matriz no manifesto do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="49762-206">The task module feature requires that the domains of any URLs you want to load are included in the `validDomains` array in your app's manifest.</span></span>

## <a name="task-module-sizing"></a><span data-ttu-id="49762-207">Dimensionamento do módulo de tarefa</span><span class="sxs-lookup"><span data-stu-id="49762-207">Task module sizing</span></span>

<span data-ttu-id="49762-208">Usando números inteiros para `TaskInfo.width` e `TaskInfo.height` definirá a altura e a largura em pixels.</span><span class="sxs-lookup"><span data-stu-id="49762-208">Using integers for `TaskInfo.width` and `TaskInfo.height` will set the height and width in pixels.</span></span> <span data-ttu-id="49762-209">No entanto, dependendo do tamanho da janela da equipe e da resolução de tela, elas serão reduzidas proporcionalmente enquanto mantêm a taxa de proporção (largura/altura).</span><span class="sxs-lookup"><span data-stu-id="49762-209">However, depending on the size of the Team's window and screen resolution they will be reduced proportionally while maintaining the aspect ratio (width/height).</span></span>

<span data-ttu-id="49762-210">Se `TaskInfo.width` e `TaskInfo.height` `"small"`se `"medium"` ou `"large"` o tamanho do retângulo vermelho na imagem acima é uma proporção do espaço disponível: 20%, 50%, 60% para `width` e 20%, 50%, 66% para. `height`</span><span class="sxs-lookup"><span data-stu-id="49762-210">If `TaskInfo.width` and `TaskInfo.height` are `"small"`, `"medium"` or `"large"` the size of the red rectangle in the picture above is a proportion of the available space: 20%, 50%, 60% for `width` and 20%, 50%, 66% for `height`.</span></span>

<span data-ttu-id="49762-211">Os módulos de tarefa invocados de uma guia podem ser redimensionados dinamicamente.</span><span class="sxs-lookup"><span data-stu-id="49762-211">Task modules invoked from a tab can be dynamically resized.</span></span> <span data-ttu-id="49762-212">Após a `tasks.startTask()` chamada, você `tasks.updateTask(newSize)` pode chamar onde as propriedades Height e Width no objeto newSize estão de acordo com a especificação TaskInfo (ex.</span><span class="sxs-lookup"><span data-stu-id="49762-212">After calling `tasks.startTask()` you can call `tasks.updateTask(newSize)` where height and width properties on the newSize object conform to the TaskInfo spec (ex.</span></span> <span data-ttu-id="49762-213">`{ height: 'medium', width: 'medium' }`).</span><span class="sxs-lookup"><span data-stu-id="49762-213">`{ height: 'medium', width: 'medium' }`).</span></span>

## <a name="task-module-css-for-htmljavascript-task-modules"></a><span data-ttu-id="49762-214">CSS do módulo de tarefas para módulos de tarefa HTML/JavaScript</span><span class="sxs-lookup"><span data-stu-id="49762-214">Task module CSS for HTML/JavaScript task modules</span></span>

<span data-ttu-id="49762-215">Os módulos de tarefa baseados em HTML/JavaScript têm acesso à área inteira do módulo de tarefa abaixo do cabeçalho.</span><span class="sxs-lookup"><span data-stu-id="49762-215">HTML/JavaScript-based task modules have access to the entire area of the task module below the header.</span></span> <span data-ttu-id="49762-216">Embora isso ofereça uma grande quantidade de flexibilidade, se você quiser que o preenchimento entre as bordas seja alinhado com os elementos de cabeçalho e evite barras de rolagem desnecessárias, será necessário fornecer o CSS correto.</span><span class="sxs-lookup"><span data-stu-id="49762-216">While that offers a great deal of flexibility, if you want padding around the edges to align with the header elements and avoid unnecessary scrollbars you'll need to provide the right CSS.</span></span> <span data-ttu-id="49762-217">Aqui estão alguns exemplos de alguns casos de uso.</span><span class="sxs-lookup"><span data-stu-id="49762-217">Here are some examples for a few use cases.</span></span>

### <a name="example-1---youtube-video"></a><span data-ttu-id="49762-218">Exemplo 1-vídeo do YouTube</span><span class="sxs-lookup"><span data-stu-id="49762-218">Example 1 - YouTube video</span></span>

<span data-ttu-id="49762-219">O YouTube oferece a capacidade de inserir vídeos em páginas da Web.</span><span class="sxs-lookup"><span data-stu-id="49762-219">YouTube offers the ability to embed videos on web pages.</span></span> <span data-ttu-id="49762-220">Usando uma página da Web de stub simples, é fácil mostrar isso em um módulo de tarefa:</span><span class="sxs-lookup"><span data-stu-id="49762-220">Using a simple stub web page it's easy to show this in a task module:</span></span>

![Vídeo do YouTube](~/assets/images/task-module/youtube-example.png)

<span data-ttu-id="49762-222">Este é o HTML desta página, sem o CSS:</span><span class="sxs-lookup"><span data-stu-id="49762-222">Here's the HTML for this page, without the CSS:</span></span>

```html
<!DOCTYPE html>
<html lang="en">
<head>
  ⋮
</head>
<body>
  <div id="embed-container">
    <iframe width="1000" height="700" src="https://www.youtube.com/embed/rd0Rd8w3FZ0" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen=""></iframe>
  </div>
</body>
</html>
```

<span data-ttu-id="49762-223">O CSS tem a seguinte aparência:</span><span class="sxs-lookup"><span data-stu-id="49762-223">The CSS looks like this:</span></span>

```css
#embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 95%;
    height: 95%;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: 10px;
    padding-bottom: 10px;
    border-style: none;
}
```

### <a name="example-2---powerapp"></a><span data-ttu-id="49762-224">Exemplo 2-PowerApp</span><span class="sxs-lookup"><span data-stu-id="49762-224">Example 2 - PowerApp</span></span>

<span data-ttu-id="49762-225">Você pode usar a mesma abordagem para incorporar um PowerApp também.</span><span class="sxs-lookup"><span data-stu-id="49762-225">You can use the same approach to embed a PowerApp as well.</span></span> <span data-ttu-id="49762-226">Como a altura/largura de qualquer PowerApp individual é personalizável, talvez seja necessário ajustar a altura e a largura para obter a apresentação desejada.</span><span class="sxs-lookup"><span data-stu-id="49762-226">As the height/width of any individual PowerApp is customizable, you may need to adjust the height and width to achieve your desired presentation.</span></span>

![Gerenciamento de ativos PowerApp](~/assets/images/task-module/powerapp-example.png)

```html
<iframe width="720" height="520" src="https://web.powerapps.com/webplayer/iframeapp?source=iframe&screenColor=rgba(104,101,171,1)&appId=/providers/Microsoft.PowerApps/apps/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"></iframe>
```

<span data-ttu-id="49762-228">E o CSS é:</span><span class="sxs-lookup"><span data-stu-id="49762-228">And the CSS is:</span></span>

```css
#embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 94%;
    height: 95%;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: 10px;
    padding-bottom: 10px;
    border-style: none;
}
```

## <a name="adaptive-card-or-adaptive-card-bot-card-attachment"></a><span data-ttu-id="49762-229">Cartão adaptável ou anexo cartão de bot de cartão adaptável</span><span class="sxs-lookup"><span data-stu-id="49762-229">Adaptive card or Adaptive card bot card attachment</span></span>

<span data-ttu-id="49762-230">Conforme observamos acima, dependendo de como você está invocando seu `card` , você precisará usar um cartão adaptável ou um anexo de cartão de bot adaptável (que é apenas um cartão adaptável disposto em um objeto Attachment).</span><span class="sxs-lookup"><span data-stu-id="49762-230">As we noted above, depending on how you're invoking your `card` you'll need to use either an Adaptive card, or an Adaptive card bot card attachment (which is just an Adaptive card wrapped in an attachment object).</span></span>

<span data-ttu-id="49762-231">Ao invocar a partir de uma guia, você precisará usar um cartão adaptável.</span><span class="sxs-lookup"><span data-stu-id="49762-231">When you're invoking from a tab, you'll need to use an adaptive card.</span></span> <span data-ttu-id="49762-232">Veja um exemplo muito simples:</span><span class="sxs-lookup"><span data-stu-id="49762-232">Here's a very simple example:</span></span>

```json
{
    "type": "AdaptiveCard",
    "body": [
        {
            "type": "TextBlock",
            "text": "Here is a ninja cat:"
        },
        {
            "type": "Image",
            "url": "http://adaptivecards.io/content/cats/1.png",
            "size": "Medium"
        }
    ],
    "version": "1.0"
}
```

<span data-ttu-id="49762-233">Ao invocar de um bot, você precisará usar um anexo de cartão de bot de cartão adaptável, como no exemplo abaixo:</span><span class="sxs-lookup"><span data-stu-id="49762-233">When you're invoking from a bot you'll need to use an Adaptive card bot card attachment as in the example below:</span></span>

```json
{
    "contentType": "application/vnd.microsoft.card.adaptive",
    "content": {
        "type": "AdaptiveCard",
        "body": [
            {
                "type": "TextBlock",
                "text": "Here is a ninja cat:"
            },
            {
                "type": "Image",
                "url": "http://adaptivecards.io/content/cats/1.png",
                "size": "Medium"
            }
        ],
        "version": "1.0"
    }
}
```

<span data-ttu-id="49762-234">Você precisará se lembrar se está invocando um módulo de tarefa contendo um cartão adaptável de um bot ou de uma guia.</span><span class="sxs-lookup"><span data-stu-id="49762-234">You'll need to remember whether you are invoking a task module containing an Adaptive card from a bot or a tab.</span></span>

## <a name="task-module-deep-link-syntax"></a><span data-ttu-id="49762-235">Sintaxe de link profundo do módulo de tarefa</span><span class="sxs-lookup"><span data-stu-id="49762-235">Task module deep link syntax</span></span>

<span data-ttu-id="49762-236">Um link profundo de módulo de tarefa é apenas uma serialização do [objeto TaskInfo](#the-taskinfo-object) com duas outras partes de informação `APP_ID` e, opcionalmente `BOT_APP_ID`, o:</span><span class="sxs-lookup"><span data-stu-id="49762-236">A task module deep link is just a serialization of the [TaskInfo object](#the-taskinfo-object) with two other pieces of information, `APP_ID` and optionally the `BOT_APP_ID`:</span></span>

`https://teams.microsoft.com/l/task/APP_ID?url=<TaskInfo.url>&height=<TaskInfo.height>&width=<TaskInfo.width>&title=<TaskInfo.title>&completionBotId=BOT_APP_ID`

`https://teams.microsoft.com/l/task/APP_ID?card=<TaskInfo.card>&height=<TaskInfo.height>&width=<TaskInfo.width>&title=<TaskInfo.title>&completionBotId=BOT_APP_ID`

<span data-ttu-id="49762-237">Confira o [objeto TaskInfo](#the-taskinfo-object) para os tipos de dados e os `<TaskInfo.url>`valores `<TaskInfo.card>`permitidos `<TaskInfo.height>`para `<TaskInfo.width>`,, `<TaskInfo.title>`, e.</span><span class="sxs-lookup"><span data-stu-id="49762-237">See [TaskInfo object](#the-taskinfo-object) for the data types and allowable values for `<TaskInfo.url>`, `<TaskInfo.card>`, `<TaskInfo.height>`, `<TaskInfo.width>`, and `<TaskInfo.title>`.</span></span>

> [!TIP]
> <span data-ttu-id="49762-238">Certifique-se de que a URL Codifique o link profundo, especialmente `card` ao usar o parâmetro (por exemplo, a [ `encodeURI()` função](https://www.w3schools.com/jsref/jsref_encodeURI.asp)de JavaScript).</span><span class="sxs-lookup"><span data-stu-id="49762-238">Be sure to URL encode the deep link, especially when using the `card` parameter (for example, JavaScript's [`encodeURI()` function](https://www.w3schools.com/jsref/jsref_encodeURI.asp)).</span></span>

<span data-ttu-id="49762-239">Aqui estão as informações sobre `APP_ID` e `BOT_APP_ID`:</span><span class="sxs-lookup"><span data-stu-id="49762-239">Here's the information on `APP_ID` and `BOT_APP_ID`:</span></span>

| <span data-ttu-id="49762-240">Valor</span><span class="sxs-lookup"><span data-stu-id="49762-240">Value</span></span> | <span data-ttu-id="49762-241">Tipo</span><span class="sxs-lookup"><span data-stu-id="49762-241">Type</span></span> | <span data-ttu-id="49762-242">Obrigatório?</span><span class="sxs-lookup"><span data-stu-id="49762-242">Required?</span></span> | <span data-ttu-id="49762-243">Descrição</span><span class="sxs-lookup"><span data-stu-id="49762-243">Description</span></span> |
| --- | --- | --- | --- |
| `APP_ID` | <span data-ttu-id="49762-244">string</span><span class="sxs-lookup"><span data-stu-id="49762-244">string</span></span> | <span data-ttu-id="49762-245">Sim</span><span class="sxs-lookup"><span data-stu-id="49762-245">Yes</span></span> | <span data-ttu-id="49762-246">A [ID](~/resources/schema/manifest-schema.md#id) do aplicativo que chama o módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-246">The [id](~/resources/schema/manifest-schema.md#id) of the app invoking the task module.</span></span> <span data-ttu-id="49762-247">A [matriz validDomains](~/resources/schema/manifest-schema.md#validdomains) no manifesto para `APP_ID` deve conter o domínio para `url` se `url` estiver na URL.</span><span class="sxs-lookup"><span data-stu-id="49762-247">The [validDomains array](~/resources/schema/manifest-schema.md#validdomains) in the manifest for `APP_ID` must contain the domain for `url` if `url` is in the URL.</span></span> <span data-ttu-id="49762-248">(O ID do aplicativo já é conhecido quando um módulo de tarefa é invocado de uma guia ou de um bot, que é o motivo `TaskInfo`pelo qual ele não está incluído no.)</span><span class="sxs-lookup"><span data-stu-id="49762-248">(The app ID is already known when a task module is invoked from a tab or a bot, which is why it's not included in `TaskInfo`.)</span></span> |
| `BOT_APP_ID` | <span data-ttu-id="49762-249">string</span><span class="sxs-lookup"><span data-stu-id="49762-249">string</span></span> | <span data-ttu-id="49762-250">Não</span><span class="sxs-lookup"><span data-stu-id="49762-250">No</span></span> | <span data-ttu-id="49762-251">Se um valor `completionBotId` for especificado, o `result` objeto é enviado por meio de uma `task/submit invoke` mensagem para o bot especificado.</span><span class="sxs-lookup"><span data-stu-id="49762-251">If a value for `completionBotId` is specified, the `result` object is sent via a a `task/submit invoke` message to the specified bot.</span></span> <span data-ttu-id="49762-252">`BOT_APP_ID`deve ser especificado como um bot no manifesto do aplicativo, ou seja, você não pode simplesmente enviá-lo para qualquer bot.</span><span class="sxs-lookup"><span data-stu-id="49762-252">`BOT_APP_ID` must be specified as a bot in the app's manifest, i.e. you can't just send it to any bot.</span></span> |

<span data-ttu-id="49762-253">Observe que é válido para `APP_ID` e `BOT_APP_ID` ser o mesmo, e em muitos casos será se um aplicativo tiver um bot, pois é recomendável usá-lo como a ID de um aplicativo, se houver um.</span><span class="sxs-lookup"><span data-stu-id="49762-253">Note that it's valid for `APP_ID` and `BOT_APP_ID` to be the same, and in many cases will be if an app has a bot since it's recommended to use that as an app's ID if there is one.</span></span>

## <a name="keyboard-and-accessibility-guidelines"></a><span data-ttu-id="49762-254">Diretrizes de teclado e acessibilidade</span><span class="sxs-lookup"><span data-stu-id="49762-254">Keyboard and accessibility guidelines</span></span>

<span data-ttu-id="49762-255">Com módulos de tarefa baseados em HTML/JavaScript, é responsabilidade garantir que o módulo de tarefa do aplicativo possa ser usado com um teclado.</span><span class="sxs-lookup"><span data-stu-id="49762-255">With HTML/JavaScript-based task modules it is your responsibility to ensure your app's task module can be used with a keyboard.</span></span> <span data-ttu-id="49762-256">Os programas de leitor de tela também dependem da capacidade de navegar usando o teclado.</span><span class="sxs-lookup"><span data-stu-id="49762-256">Screen reader programs also depend on the ability to navigate using the keyboard.</span></span> <span data-ttu-id="49762-257">Como uma questão prática, isso significa duas coisas:</span><span class="sxs-lookup"><span data-stu-id="49762-257">As a practical matter this means two things:</span></span>

1. <span data-ttu-id="49762-258">Usando o [atributo TabIndex](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/tabindex) em suas marcas HTML para controlar quais elementos podem ser focados e se/onde ele participa na navegação de teclado seqüencial (normalmente com as teclas <kbd>Tab</kbd> e <kbd>Shift-Tab</kbd> ).</span><span class="sxs-lookup"><span data-stu-id="49762-258">Using the [tabindex attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/tabindex) in your HTML tags to control which elements can be focused and if/where it participates in sequential keyboard navigation (usually with the <kbd>Tab</kbd> and <kbd>Shift-Tab</kbd> keys).</span></span>
2. <span data-ttu-id="49762-259">Tratamento da chave <kbd>ESC</kbd> no JavaScript para seu módulo de tarefa.</span><span class="sxs-lookup"><span data-stu-id="49762-259">Handling the <kbd>Esc</kbd> key in the JavaScript for your task module.</span></span> <span data-ttu-id="49762-260">Veja um exemplo de código que mostra como fazer isso:</span><span class="sxs-lookup"><span data-stu-id="49762-260">Here's a code sample showing how to do this:</span></span>

  ```javascript
  // Handle the Esc key
  document.onkeyup = function(event) {
  if ((event.key === 27) || (event.key === "Escape")) {
    microsoftTeams.submitTask(null); // this will return an err object to the completionHandler()
    }
  }
  ```

<span data-ttu-id="49762-261">O Microsoft Teams garantirá que a navegação pelo teclado funcione corretamente do cabeçalho do módulo de tarefa no HTML e vice-versa.</span><span class="sxs-lookup"><span data-stu-id="49762-261">Microsoft Teams will ensure that keyboard navigation works properly from the task module header into your HTML and vice-versa.</span></span>

## <a name="task-module-samples"></a><span data-ttu-id="49762-262">Amostras de módulo de tarefa</span><span class="sxs-lookup"><span data-stu-id="49762-262">Task module samples</span></span>

* [<span data-ttu-id="49762-263">Exemplo de Node. js/TypeScript</span><span class="sxs-lookup"><span data-stu-id="49762-263">Node.js/TypeScript sample</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-task-module-nodejs)
* [<span data-ttu-id="49762-264">Exemplo de/.NET C#</span><span class="sxs-lookup"><span data-stu-id="49762-264">C#/.NET sample</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-task-module-csharp)
